<resources>
    <!-- Base application theme. -->
    <style name="Theme.Metanoia" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Splash Screen theme. -->
    <style name="Theme.Metanoia.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_metanoia_transform</item>
        <item name="windowSplashScreenBackground">#1a1b26</item>
        <item name="postSplashScreenTheme">@style/Theme.Metanoia</item>
    </style>
</resources>
